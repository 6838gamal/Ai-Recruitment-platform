Update SENSITIVE list and add optional configuration for extra fields to hide.

- Add DEFAULT_SENSITIVE in inspect_model and allow app config override via environment var DYNAMIC_TEMPLATES_SENSITIVE (comma-separated).
